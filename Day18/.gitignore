# Terraform state and local cache — never commit these
.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.*.backup
.terraform.lock.hcl
errored.tfstate

# Terraform variable files that may contain secrets
*.tfvars
*.tfvars.json
!example.tfvars

# Go test binary and cache
test/vendor/
*.test
